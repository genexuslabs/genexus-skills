# @genexus/mercury/components/chat/message-metadata

## MercuryChatMessageMetadata

```ts
type MercuryChatMessageMetadata = {
  agentName: string;
  time?: string;
};
```

Type for chat message metadata when using Mercury’s chat render. Use with Chameleon chat’s message metadata handling
