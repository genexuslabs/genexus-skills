# @genexus/mercury/register-mercury.js

## registerMercury

```ts
registerMercury(): void
```

One-time registration of Mercury assets and callbacks. Call once during app bootstrap (e.g. in `main.ts` or equivalent) before using Mercury components or `getBundles`
