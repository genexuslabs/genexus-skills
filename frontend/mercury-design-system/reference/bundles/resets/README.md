# Resets

Global CSS resets. These are not bundles requested by name via `getBundles`; they are injected by the Mercury build (Vite plugin or CLI) when `base/base` and `resets/box-sizing` are inlined

## Files

- [box-sizing.css](box-sizing.css) — Box-sizing reset (`* { box-sizing: border-box }`)
