# MBR Properties

## (root)

- `ObjName` (Text) — Name
- `ObjDesc` (Text) — Description. Describe the content or purpose of the element

## (root)

- `ObjId` (Integer) — ObjId [hidden]
- `ObjIsStyle` (Boolean) — IsStyle. Default: `false` [hidden]
- `Folder` (Custom) — Folder
- `Theme` (Custom) — Theme [hidden]
- `STD_FUNC_OBJECT` (Combo) — Standard Functions. Standard functions checking. Default: `UMPV`. Values: Use Environment property value, Only standard functions, Allow non-standard functions [hidden]

