# IMAGE Properties

## (root)

- `ObjName` (Text) — Name
- `ObjDesc` (Text) — Description. Describe the content or purpose of the element

## (root)

- `IsThemeDependant` (Boolean) — Theme Dependant
- `IsLanguageDependant` (Boolean) — Language Dependant
- `ImageDescriptors` (Text) — ImageDescriptors [hidden]
- `DefaultImage` (Custom) — DefaultImage [hidden]

